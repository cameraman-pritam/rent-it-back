{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Server (Bun)",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun run dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Debug",
      "type": "chrome",
      "request": "launch",
      "url": "http://rent-back.localhost:1355",
      "webRoot": "${workspaceFolder}/src",
      "sourceMaps": true,
      "timeout": 30000
    },
    {
      "name": "Build and deploy",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun run build && firebase deploy",
      "cwd": "${workspaceFolder}"
    }
  ]
}
